USE workbook;


use1-1

1
	SELECT #取得
		* #全件
	FROM
		books #本テーブル
	;


2
	SELECT #取得
		* #全件
	FROM
		author #著作テーブル
	;


3
	SELECT #取得
		* #全件
	FROM
		sales #売上テーブル
	;


4
	SELECT #取得
		* #「+」ではない
	FROM
		books #booksテーブル
	; #「:」ではない


1-2

1
	SELECT #取得
    	title #タイトル
    FROM
    	books #本テーブル
    ;
    
    
2
	SELECT #取得
    	p_name, #商品名
    	price #単価
    FROM
    	product #商品テーブル
    ;
    
    
3
	SELECT #取得
    	l_name, #社員の氏
    	f_name, #社員の名
    	class #役職
    FROM
    	employee #社員テーブル
    ;
    
    
4
	SELECT #「SELECTSは誤字」
    	title, #「，が抜けている」
    	publish #出版社
    FROM
    	books #本テーブル
    ;
    
    
1-3
1
	SELECT DISTINCT #重複除いて取得
    	prefecture #都道府県名
    FROM
    	quest #アンケート回答テーブル
    ;
    
    
2
	SELECT DISTINCT #重複除いて取得
    	referer #リンク元URL
    FROM
    	access_log #アンケート回答テーブル
    ;
    
    
3
	SELECT DISTINCT #重複除いて取得
    	l_name, #ユーザー氏
    	f_name #ユーザー名
    FROM
    	usr #ユーザーテーブル
    ;


4
	SELECT DISTINCT #「ALLではなくDISTINCT」
    	class #「,とdepart_idは不要」
    FROM
    	employee #社員テーブル
    ;
    
    
1-4
1
	SELECT #取得
		answer1, #回答1
		answer2 #回答2
	FROM
		quest #アンケート回答テーブル
	WHERE
		age >= 20 #20歳以上に絞る
	;
	
	
2
	SELECT #取得
		isbn, #ISBNコード
		title, #タイトル
		publish #出版社
	FROM
		books #本テーブル
	WHERE
		publish in ('日経BP','翔泳社') #(日経BP,翔泳社で絞り込む)
	;


3
	SELECT #取得
		l_name, #氏
		f_name, #名
		email #メールアドレス
	FROM
		usr #ユーザーテーブル
	WHERE
		prefecture NOT IN ('東京都') #(東京に住んでいない人で絞り込む)
	;
	
	
4
	SELECT #取得
		name, #名
		sex, #性別
		prefecture #出身
	FROM
		quest #アンケート回答テーブル
	WHERE
		age BETWEEN 30 AND 39 #(30～40歳未満で絞り込む)
	;


5
	SELECT #取得
		name, #名
		answer1, #回答1
		answer2 #回答2
	FROM
		quest #アンケート回答テーブル
	WHERE
		answered >= '2013-01-01' #(回答時間2013-01-01で絞り込む)
	;



6
	SELECT #取得
		user_id, #ユーザーID
		isbn #ISBNコード
	FROM
		rental #貸し出し記録テーブル
	WHERE
		returned = 1 #(「1」貸し出し中で絞り込む)
	;
	
	